// import mongoose from "mongoose";
// import { router } from ".";

// import { t } from "./trpc";
// import { v4 } from 'uuid';

// connect().then(()=>{
//   const createCaller = t.createCallerFactory(router);
  
  
//   const caller = createCaller({});
  
//   caller.posts.createPost({ userId: v4(), content: "hello" }).then(console.log)
// })